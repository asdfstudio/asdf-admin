import Layout from "../src/ui-components/layout";
import Portfolio from "../src/Pages/portfolio";

export default () => {
    return (
        <Layout>
            <Portfolio />
        </Layout>
    );
}